cc = Obj(
  flags=[ '--std=c++14', '-x', 'c++' ],
  hdrs_flags=[ '-MM', '-MG' ],
  incl_dirs=[]
)

has_env = 'INCL_ENV_VAR' in os.environ
cc.incl_dirs += [os.environ['INCL_ENV_VAR']] if has_env else []

link = Obj(
  flags=[ ],
  libs=[ 'stdc++' ],
  static_libs=[],
  lib_dirs=[]
)

make = Obj(
  tool='make',
  flags=[ '-s' ],
  force_flag='-B',
  all_pseudo_target='all'
)
